Keep large videos out of git. Put a short local clip here for Checkpoint 2, or let `scripts/fetch-sample-clip.ps1` download one.

`example_tracking.json` is the V1 Python→C++ contract fixture. Real `tracking.json` files go under `results/`.
